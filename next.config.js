/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  images: {
    unoptimized: true, // Deshabilita la optimización de imágenes para exportación estática.
  },
  // Aquí puedes mantener cualquier otra configuración que necesites.
}

module.exports = nextConfig;
